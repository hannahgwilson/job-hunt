# Secrets & local env — never commit
.env
.env.local
.env.*.local
*.secret

# Real résumé — long-form personal data the agent scores against. Only the
# placeholder (resume/resume.example.md) is committed. See semantic/metrics/priority_score.yaml.
resume/resume.md

# Node / web build
node_modules/
web/node_modules/
web/dist/

# Supabase local
.branches/
.temp/

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
